<% if policy(Reg.new).create? %>
  <%= link_to "New Regs", new_state_reg_path(@state), class: 'btn btn-success' %>
<% end %>


<div class="container">
  <div class="col-md-11">
    <% @regs.each do |reg| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to reg.name, [@state, reg] %>

            <% if policy(reg).destroy? %>
              <%= link_to "", state_reg_path(@state, reg), method: :delete, class: 'pull-right glyphicon glyphicon-remove',
            data: { confirm: 'Are you sure you want to delete this?'} %>
            <% end %>

            <% if policy(reg).edit? %>
              <%= link_to "", edit_state_reg_path(@state, reg), class: 'pull-right glyphicon glyphicon-pencil' %>
            <% end %>

          </h4>
        </div>
      </div>
    <% end %>
  </div>
</div>
