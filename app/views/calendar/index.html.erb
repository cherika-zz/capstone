<div class="container">
  <div class="col-md-11">
    <% @states.each do |state| %>
      <div class="media">
        <div class="media-body">
          <h4 class="media-heading">
            <%= link_to state.name, state %>
          </h4>
        </div>
      </div>
    <% end %>
  </div>
</div>
